#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 *main - Entry point
  *
 *Return: Always 0 (success)
 *Description: This program prints all letters
 *Return: Returns 0 
 */
int main(void)
{
char start = 'z';
char aux;
for (aux = 'a'; aux <= start; aux++)
char letter = 'a';

while (letter <= 'z')
{
putchar(aux);
putchar(letter);
letter++;
}
putchar('\n');
{
putchar(aux);
}
return (0);
}
