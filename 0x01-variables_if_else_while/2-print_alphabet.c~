#include <stdio.h>

/**
 * main - alphabet in lowercase, followed by a new line
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
 int char;
 for (char = "a", char < "z", char++)
 {
 putchar (char);
 putchar (\n);
 }
 return (0);
}
      
