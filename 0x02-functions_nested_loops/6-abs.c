#include "holberton.h"
/**
* _abs - tells the absolute value of an integer
* @i: int type number
* Return: absolute value of @n
*/
int _abs(int i)
{
if (i < 0)
{ return (i * -1);
}
else
{
return (i);
}
}
