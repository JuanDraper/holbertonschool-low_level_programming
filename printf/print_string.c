#include "holberton.h"

/**
 * printstr- print str.
 * @:a chupala printf
 * Return: number str for print.
 */
int printstr(char *p)
{
	char *str = p;
	int cont;

	if (str == NULL)
		str = "(null)";

	return (cont);
}
